# Optional public analytics configuration
# Set this in Vercel only after the analytics domain is configured.
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=d3vonn.io

# Optional future status integrations
# Keep provider API keys server-side only. Do not prefix secrets with NEXT_PUBLIC.
RAILWAY_STATUS_URL=
SUPABASE_HEALTH_URL=
PINECONE_HEALTH_URL=
HERMES_HEARTBEAT_URL=
